/**
 * Translates JavaScript scalars and objects into JSON
 * compatible objects. This is done mostly to transmit
 * data into commonly used protocols, i.e. HTTP, as JSON objects.
 * @param value
 */
export function serialize(value: unknown): unknown {
  /* put implementation here */
}

/**
 * Parses JSON scalars and objects into JavaScript
 * compatible objects. This is done mostly to accept
 * data from commonly used protocols, i.e. HTTP, as JSON objects.
 * @param value
 */
export function deserialize(value: unknown): unknown {
  /* put implementation here */
}
