/**
 * This buffer id setup is used to hold object metadata.
 * It holds object information through numeric representation.
 * @param type
 */
export default function (type?: number): Buffer {
  /* put implmentation here */
}
